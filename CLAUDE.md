# Project instructions

## Git commits

Auto-commit is authorized for this project: after making a set of related file
changes, create a git commit for them without asking for confirmation first.
Still follow the standard git safety rules (no force-push, no --no-verify, no
amending, review staged files before committing). Do not push to the remote
unless explicitly asked.
